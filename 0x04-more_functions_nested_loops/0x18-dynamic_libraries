#include "main.h"
/**
 * _isdigit - A function that checks for a digit (0 through 9).
 * @c: An input integer
 * Return: 1 if c is a digit or 0 otherwise
 */
int _isdigit(int c)
{
char s = '0';
int isdigit = 0;

for (; s <= '9'; s++)
{
if (s == c)
{
isdigit = 1;
break;
}
}

return (isdigit);
}
