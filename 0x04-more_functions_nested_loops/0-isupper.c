#include "main.h"
/**
 *_isupper -A function that checks for uppercase char
 *@c: An input char
 *Return 1 is c is uppercase n 0 if otherwise
 **/

int_isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);

else
return (0);
}
