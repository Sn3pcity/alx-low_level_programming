#include <stdio.h>
#include <stdlib.h>
#include "variadic_functions.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int sum_them_all(const unsigned int n, ...)
{
if (n == 0)
return (0);
else
return (1);
}
